---
- name: Restart Mastodon services
  ansible.builtin.systemd:
    name: "{{ item }}"
    state: restarted
  loop:
    - mastodon-web
    - mastodon-sidekiq
    - mastodon-streaming
  become: true
  failed_when: false
  listen: "Restart Mastodon services"
